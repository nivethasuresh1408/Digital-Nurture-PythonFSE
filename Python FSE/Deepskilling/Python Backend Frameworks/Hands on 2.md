# Hands-On 2: Django Models, ORM & Admin Interface

## Task 1: Define Models and Run Migrations

Implemented the Department, Course, Student, and Enrollment models with appropriate fields, relationships, `__str__()` methods, and the `unique_together` constraint. Generated and applied database migrations successfully using Django's migration system.

<img width="1920" height="1020" alt="image" src="https://github.com/user-attachments/assets/6a1e41fb-f4ce-4aec-8964-a1a54b934f9a" />


---

## Task 2: Django ORM Queries

Created sample Department, Course, and Student records using the Django ORM shell. Performed CRUD operations including `filter()`, `annotate()`, `select_related()`, and updated department budgets using `F()` expressions.

<img width="1920" height="1020" alt="image" src="https://github.com/user-attachments/assets/d6c2d685-537c-4db1-a93a-0cd2a9665f26" />


---

## Task 3: Django Admin Interface

Registered all models in the Django Admin site and customized the Course admin using `list_display`, `search_fields`, and `list_filter`. Added sample records through the admin interface and verified the enrollment uniqueness constraint.

<img width="1920" height="1020" alt="image" src="https://github.com/user-attachments/assets/1479970e-f025-4c03-931b-6e3690587a93" />
